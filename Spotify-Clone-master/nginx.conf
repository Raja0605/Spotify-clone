server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        try_files $uri $uri/ =404;
    }

    # Cache static assets for performance
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|mp3)$ {
        expires 30d;
        add_header Cache-Control "public";
    }
}
